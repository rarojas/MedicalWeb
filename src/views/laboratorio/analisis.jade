extends ../funcions

block content
  #inicio
    .container
      .row
        .col-md-12.col-sm-12
          .div-trans.text-center
            h3 Analisis
            .col-md-12.col-sm-12
                table(class="table table-striped table-bordered table-hover ")
                  thead
                    tr
                      th  Paciente
                      th  impresion
                      th  descripcion
                      th  Acciones
                  tbody
                    tr(ng-repeat="consulta in vm.analisis" style="color:black;")
                      td {{consulta.paciente.nombre}}
                      td {{consulta.impresion}}
                      td {{consulta.descripcion}}
                      td
                        a.btn.btn-primary(href="#/realizarAnalisis/{{consulta.idSolicitud}}") Analisis
              
